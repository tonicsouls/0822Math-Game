import React from 'react';

interface DPadProps {
    onDirectionPress: (dx: number, dy: number) => void;
}

const DPad: React.FC<DPadProps> = ({ onDirectionPress }) => {
    return null;
};

export default DPad;